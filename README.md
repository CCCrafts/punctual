# Host groups — screenshots (2026-09-02)

Captured on a local `wrangler dev` seeded with a four-member team (Alice admin, Bob, Carol, Dan), a collective "Support call" with Alice required and Bob/Carol optional (Bob on his admin-created "Support hours" schedule), and a round-robin "Sales intro". Light, dark and 390px mobile.

| File | What |
|---|---|
| `event-type-hosts-*.png` | Edit event type: the Hosts block (attendance, per-host schedule, "+ New schedule for…") |
| `availability-team-events-*.png` | Availability page: "Team events" section |
| `teams-roles-*.png` | Teams page: roles, promote/demote, per-member Availability link |
| `member-availability-on-behalf-*.png` | Admin managing Bob's schedules on his behalf ("set up by" badge on Bob's side) |
| `booking-page-collective-*.png` | Team-headed booking page, "You'll meet Alice Ivanova. Bob Chen and Carol Diaz join when free" |
| `booking-page-round-robin-*.png` | "With one of …" pool wording |
| `og-personal.png`, `og-collective.png`, `og-round-robin.png` | Social cards: single face, stacked faces with first names, team with +N |

Linear: CCC-686 (umbrella), CCC-688, CCC-689, CCC-694.
