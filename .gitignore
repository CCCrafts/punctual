# Local working notes (docs/ itself is public — self-hosting guide lives there)
notes/
scratch/

# Secrets & env
.env
.env.*
.dev.vars

# Dependencies & build
node_modules/
dist/
.wrangler/

# OS / editor
.DS_Store
